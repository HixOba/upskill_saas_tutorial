<div class="container">
  <div class="row">
    <div class="col-md-4">
      <div class="text-center">
        <% if params[:plan] == '2' %>
          <h1>Pro Membership</h1>
          <p>Sign up for £10/month for the Pro membership and get full access to the community and contact to other members!</p>
        <% else %>
          <h1>Basic Membership</h1>
          <p>Sign up for free and get basic access to the community!</p>
        <% end %>
      </div>
      
      <div class="well" style="margin-top: 50px">
        <h2>Sign up</h2>
        
        <% if params[:plan] == '2' %>
          <%= render "pro_form" %>
        <% else %>
          <%= render "basic_form" %>
        <% end %>
        
        <%= render "devise/shared/links" %>
      </div>
    </div>
  </div>
</div>
